{
  "name": "todo-node-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    /* This is tweking the test command a liitle bit as
      first command is to assign NODE_ENV to test in Linux as needed in Heroku
      second command is for Windows to run locally in the machine
      third is common for both the previous commands to run mocha test case */
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js",
    "test-1": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "11.10.1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.0",
    "lodash": "^4.17.11",
    "mongodb": "^3.2.6",
    "mongoose": "^5.5.11"
  },
  "devDependencies": {
    "expect": "^24.8.0",
    "mocha": "^6.1.4",
    "nodemon": "^1.19.1",
    "supertest": "^4.0.2"
  }
}
